for(var row=1;row<=4;row++){
    var ptr="";
    for(var col=1;col<=row;col++){
        //prt+="#"// if  print the star;
        //ptr+=col // if print the number;
        //ptr+=row; // if print the number
    }
    console.log(ptr);
}